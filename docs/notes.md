## JS

- Better to include file.js at the end of the html bod, because html loads first, ensuring the script can safely access all elements without delays or errors.
`<script src="script.js"></script>`

- To select an HTML element, JavaScript most often uses the document.getElementById() method.


